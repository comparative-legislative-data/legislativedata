.DS_Store
*.env
legdatavps/
sources/phd/*.xlsx

# Python bytecode, left by importing the tools directly
__pycache__/
*.pyc

# Local build of the tools environment (tools/requirements.txt)
venv/

# Local run of the site (tools/deploy_site.sh builds its own on the machine)
site/.venv/

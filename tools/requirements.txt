# Environment for the tools that read a source: tools/extract_factsheet.py reads
# the SPICe factsheets, tools/compare_sources.py reads the PhD dataset.
#
# Pinned, not floating. The reconciliation argument in DECISIONS.md rests on the
# extractor being deterministic: a factsheet must yield the same 73 rows on the
# VPS as it does anywhere else, or "it reconciles" means nothing. A pdfminer
# change to how a ruled table is walked would alter row counts silently.
#
# Rebuild:  python3 -m venv venv && venv/bin/pip install -r tools/requirements.txt
# Verified against Python 3.12 (macOS) and 3.13.5 (Debian 13, the VPS).

pdfplumber==0.11.10

# The same argument covers the comparison, which since db/044 is a gate every
# session must pass before it can be admitted: a session must yield the same
# list of differences wherever the comparison is run, or the gate means nothing.
openpyxl==3.1.5

# Resolved dependencies, pinned so the tree cannot drift underneath the pins above.
cffi==2.1.1
charset-normalizer==3.5.1
cryptography==50.0.1
pdfminer.six==20260107
pillow==12.3.0
pycparser==3.0
pypdfium2==5.13.0
et-xmlfile==2.0.0
